using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;
using UnityEngine.UI;
using System;

public class ReadWin : MonoBehaviour
{
    public Text p;


    private void Start()
    {
        /*
        string win;

        StreamReader reader = new StreamReader("Assets/Resources/wygrywa.txt");
        win = reader.ReadLine();
        Debug.Log(win);

        string player = win.Substring(win.Length-1);
        
        int x = Int32.Parse(player);
        Debug.Log(player+" "+ x);


        if (x == 2)
        {
            p.color = new Color(192f, 41f, 41f, 255f);
            p.text = "Niebieski";
        }
        else if(x == 1)
        {
            p.color = new Color(42f, 111f, 255f, 255f);
            p.text = "Niebieski";
        }
        
        */




    }
    




}
